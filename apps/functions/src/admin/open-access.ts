/**
 * When `true`, Back Office skips staff allowlist (emergency review only).
 * Production: keep `false` — Google Sign-In + staffUsers required.
 * Must match web `ADMIN_OPEN_ACCESS`.
 */
export const ADMIN_OPEN_ACCESS = false;

