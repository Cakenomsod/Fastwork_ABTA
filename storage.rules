rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Phase 1: uploads go through backend — no direct public write
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
